bin = `node_module/.bin`;

Concat: `cat $in > $out`;
Coffee: `$bin/coffee $in > $out`;
Minify: `$bin/minify < $in > $out`;

src/**/*.coffee
    Coffee => build/**/*.js;

build/**/*.js src/**/*.js
    Concat -> dist/concat.js;

dist/*.js
    Minify => dist/*.min.js
    :: all `Update all files`;
